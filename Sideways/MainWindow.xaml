<Window x:Class="Sideways.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sideways="clr-namespace:Sideways"
        Title="MainWindow"
        Width="800"
        Height="450"
        Background="#FF050515"
        mc:Ignorable="d">
    <Window.DataContext>
        <sideways:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
            <!--<ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />-->
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <TextBlock HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="Gray"
                       Text="W" />
            <sideways:Chart x:Name="WeeklyChart">
                <sideways:CandleSticks ItemsSource="{Binding CurrentSymbol.Days}"
                                       CandleInterval="Week"
                                       Time="{Binding EndTime}" />
            </sideways:Chart>
        </Grid>

        <Grid Grid.Column="1">
            <TextBlock HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="Gray"
                       Text="{Binding CurrentSymbol.Symbol}" />
            <sideways:Chart x:Name="DailyChart">
                <sideways:CandleSticks ItemsSource="{Binding CurrentSymbol.Days}"
                                       Time="{Binding EndTime}" />
            </sideways:Chart>
        </Grid>

        <!--<Grid Grid.Column="2">
            <TextBlock HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="Gray"
                       Text="h" />
            <sideways:Chart x:Name="HourlyChart">
                <sideways:CandleSticks ItemsSource="{Binding CurrentSymbol.Minutes}"
                                       CandleInterval="Hour"
                                       Time="{Binding EndTime}" />
            </sideways:Chart>
        </Grid>

        <Grid Grid.Column="3">
            <TextBlock HorizontalAlignment="Left"
                       VerticalAlignment="Top"
                       Foreground="Gray"
                       Text="m" />
            <sideways:Chart x:Name="MinuteChart">
                <sideways:CandleSticks ItemsSource="{Binding CurrentSymbol.Minutes}"
                                       Time="{Binding EndTime}" />
            </sideways:Chart>
        </Grid>-->
    </Grid>
</Window>
